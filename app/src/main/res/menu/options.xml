<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/menuItemClearData"
        android:checkable="false"
        android:enabled="true"
        android:icon="@drawable/ic_delete_forever_black_24dp"
        android:iconTint="@color/colorPrimary"
        android:onClick="showClearDataDialog"
        android:title="@string/clear_data"
        android:visible="true"
        app:showAsAction="never" />

    <item
        android:id="@+id/menuItemAbout"
        android:checkable="false"
        android:enabled="true"
        android:icon="@drawable/ic_help_black_24dp"
        android:iconTint="@color/colorPrimary"
        android:onClick="showAboutTheApp"
        android:title="@string/about_the_app"
        android:visible="true"
        app:showAsAction="never" />
</menu>